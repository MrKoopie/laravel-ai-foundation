# Claude Code Instructions

Use [AGENTS.md](AGENTS.md) for the full repository instructions.

This file exists because Laravel Boost uses `CLAUDE.md` as the default guideline path for the `claude_code` agent. Most other Boost-supported agents use `AGENTS.md`.
